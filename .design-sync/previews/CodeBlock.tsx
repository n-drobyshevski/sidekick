import { CodeBlock } from "@wiz-sidekick/design-system";

export const Default = () => (
  <CodeBlock label="Reproduce" text={"npm run probe -- --schema\n# copy the OBJECT_FILTERS entry, never infer it"} />
);
