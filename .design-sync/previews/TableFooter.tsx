import { TableFooter } from "@wiz-sidekick/design-system";

export const Default = () => (
  <div style={{ width: 520 }}>
    <TableFooter page={2} pageCount={12} total={573} pageSize={50} onPage={() => {}} onPageSize={() => {}} />
  </div>
);
