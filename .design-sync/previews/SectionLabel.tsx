import { SectionLabel } from "@wiz-sidekick/design-system";

export const Default = () => (
  <SectionLabel text="Remediation coverage" help="The share of findings closed within their SLA window." />
);
