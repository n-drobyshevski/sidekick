# Local development and test only. A Databricks cluster already provides Spark, Delta, pandas
# and matplotlib -- do not install pyspark on the cluster, it will shadow the runtime's build.
pyspark>=3.5
requests>=2.31
pandas>=2.0
matplotlib>=3.7
pytest>=8.0
