// One-time environment setup. Filled in across Phase 2/3: creates the ledger
// spreadsheet tabs, Drive folders, and the daily scan trigger, and records their IDs
// in Script Properties.
export function setup(): string {
  return "setup: not yet implemented (Phase 2)";
}
