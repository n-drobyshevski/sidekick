{
  "name": "wiz-sidekick-gas",
  "version": "0.1.0",
  "private": true,
  "description": "Wiz vulnerability dashboard as a Google Apps Script web app",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node dev/serve.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test && npm run build",
    "push": "npm run check && clasp push",
    "deploy": "npm run push && clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^3.4.0",
    "@types/google-apps-script": "^2.0.12",
    "@types/node": "^22.20.1",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "chart.js": "4.5.1"
  }
}
